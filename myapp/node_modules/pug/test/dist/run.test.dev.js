'use strict'; // even and odd tests are arbitrarily split because jest is faster that way

var fs = require('fs');

var assert = require('assert');

var mkdirp = require('mkdirp').sync;

var runUtils = require('./run-utils');

var pug = require('../');

var cases = runUtils.findCases(__dirname + '/cases');
var es2015 = runUtils.findCases(__dirname + '/cases-es2015');
mkdirp(__dirname + '/output');
describe('test cases', function () {
  cases.forEach(function (test, i) {
    runUtils.testSingle(it, '', test);
  });
});